# 工业界推荐系统学习笔记

> 基于王树森（小红书）推荐系统课程整理。
> 课程来源：[GitHub - wangshusen/RecommenderSystem](https://github.com/wangshusen/RecommenderSystem)

---

## 第一章：推荐系统基础概念

> 对应文件：`Notes/01_Basics.pdf`，`Slides/01_Basics_01~03.pdf`

### 1.1 推荐系统链路（漏斗架构）

推荐系统的核心是一个**多阶段过滤漏斗**，将海量物品逐步筛选到最终展示的几十条。

| 阶段 | 输入规模 | 输出规模 | 方法 | 目的 |
|------|---------|---------|------|------|
| **召回** | 数十亿 | 数千 | 多路并行（协同过滤、双塔、关注作者等） | 粗粒度候选集 |
| **粗排** | 数千 | 数百 | 小型神经网络 + 统计特征 | 快速初步筛选 |
| **精排** | 数百 | 数百 | 大型神经网络，多目标打分 | 精细化排序 |
| **重排** | 数百 | 数十 | MMR/DPP 多样性采样 + 规则过滤 | 多样性 + 业务干预 |

**召回通道是并行的**，每个通道独立召回约100条，汇总后进入粗排。

**精排输出的分数**是多个预估值的加权融合，包括：
- 点击率（CTR）
- 点赞率、收藏率、转发率
- 评论率

**重排**的主要工作：
- 多样性采样（避免同类内容扎堆）
- 规则去重、打散
- 插入广告和运营内容

---

### 1.2 核心指标体系

推荐系统的指标分为**核心指标**和**非核心指标**。优化方向以核心指标为准。

#### 用户规模指标
- **DAU**（日活跃用户）：当天使用过 App 的用户数
- **MAU**（月活跃用户）：当月使用过 App 的用户数
- **FDAU**（Feed DAU）：使用推荐功能的用户数
- **SDAU**（Search DAU）：使用搜索功能的用户数
- **推荐渗透率** = FDAU / DAU（衡量推荐功能的覆盖率）

注：SDAU + FDAU > DAU（用户可能同时使用两个功能），因此两者可独立优化。

#### 消费指标（核心）
- **人均阅读量**：每个用户每天平均看多少条内容
  - 双列信息流：点击才算阅读
  - 单列视频流：每次滑动算一次阅读
- **人均使用推荐时长**：每个用户每天在推荐 Feed 上花多少时间（不含搜索、聊天等）

> **Trade-off 注意**：两者常常反向变化。例如推荐长视频效果好 → 时长增加但阅读量减少。需要专家分析合理的兑换比例（如 +1条阅读 vs -1.2分钟时长）。

#### 留存指标（核心）

两种留存的定义不同：

- **次n留（n-day rolling retention）**：第 t 天的用户，在 [t, t+n] 内有任意一天使用即算留存
- **第n留（nth-day retention）**：第 t 天的用户，恰好在第 t+n 天使用才算留存

**LT_n（生命周期价值）**：
```
LT_n = 1 + Σ(i=1 to n) r_i    其中 r_i 是第i留的留存率
```
取值范围 [1, n+1]，常用 LT_7、LT_28。

规律：次n留 ≥ 第n留；留存率随 n 增大单调不递减。

#### 转化漏斗指标
```
曝光 → 点击 → 滑动到底 / 点赞 / 收藏 / 转发 / 评论
```

- **有效点击**：在详情页停留 > τ 秒，或产生任意交互行为（点赞、收藏等）
- 有效 CTR ≤ 原始 CTR

互动指标（点赞、收藏、转发）是非核心指标，通常与核心指标正相关，但有时存在 trade-off，监控而非主要决策依据。

---

### 1.3 A/B 测试框架

#### 随机分桶原理
- 将所有用户划分为 b 个桶（如 b=10，每桶10%流量）
- 用 hash 函数将用户 ID 映射为整数，均匀分配到 b 个桶
- 每个桶在各维度（DAU、留存、消费、互动）上的分布必须高度一致（精度0.01%级别）

#### 实验组设置
- **对照组**：1个桶，使用基线算法
- **实验组**：多个桶，每个桶测试一个变体
- 计算实验组 vs 对照组的 diff，diff 显著正向 → 扩大流量

#### 分层实验（解决"实验容量"问题）

不分层的问题：10个桶只能同时跑9个实验。

**分层实验**允许召回、粗排、精排、重排各自独立使用全部10个桶，大幅提升实验容量。

核心设计原则：
- **同层互斥**：同一层的桶不重叠，`U_i ∩ U_j = ∅`
  - 目的：避免两个相互影响的实验同时作用于同一用户（1+1 ≠ 2）
- **不同层正交**：不同层的桶均匀重叠，`|U_i ∩ V_j| = n/100`
  - 目的：不同层的实验相互独立（1+1 = 2），节省实验流量

例：召回层4个桶的实验 → 只占用召回层容量，粗排层仍有10个桶可用。

#### Holdout 机制
**目的**：单独衡量整个推荐部门对业务指标的贡献。

- 预留10%用户作为 Holdout 桶，完全不接受任何新实验
- 其余90%用户进行各层的分层实验
- 周期结束时，比较 10% Holdout vs 90% 实验组的指标差异（需归一化）= 整个部门的净贡献

**Holdout 重置流程**：
1. 周期结束 → 将新策略全量推送到100%用户
2. 随机重新划分：新的10% Holdout vs 90% 实验组
3. 新周期开始时两组 diff 接近零，随着新实验上线，diff 逐渐拉开

#### 实验推全 + 反转实验

**问题**：某个实验效果好，想尽快全量，但部分指标（留存）需要长期观察。

**解决方案**：
1. 新建推全层（90%用户）与现有各层正交
2. 推全层中：85% 使用新策略，5% 保留旧策略（**反转桶**）
3. 长期监控反转桶 vs 新策略桶的 diff
4. 既实现了快速推全，又保留了长期观测窗口

---

## 第二章：召回

> 对应文件：`Slides/02_Retrieval_01~12.pdf`

召回的目标是从数十亿物品中快速找到用户可能感兴趣的数千个候选。通常有**多路召回通道**并行运行。

---

### 2.1 ItemCF（基于物品的协同过滤）

> 对应文件：`Slides/02_Retrieval_01.pdf`

#### 核心思想
```
用户喜欢 item₁
item₁ 与 item₂ 相似        →  推荐 item₂
用户没看过 item₂
```

系统如何判断两个物品相似？——**看过 item₁ 的用户也看过 item₂**（受众重叠）。

#### 物品相似度（余弦相似度）

设：
- W₁ = 喜欢 item₁ 的用户集合
- W₂ = 喜欢 item₂ 的用户集合
- V = W₁ ∩ W₂（两者的交集）

**简化版（不考虑喜欢程度）：**
```
sim(i₁, i₂) = |V| / sqrt(|W₁| · |W₂|)
```

**完整版（考虑喜欢程度 like 分数）：**
```
sim(i₁, i₂) = Σ_{v∈V} like(v, i₁) · like(v, i₂)
               ─────────────────────────────────────────
               sqrt(Σ_{u₁∈W₁} like²(u₁,i₁)) · sqrt(Σ_{u₂∈W₂} like²(u₂,i₂))
```
这本质上是两个物品向量（每个维度对应一个用户）的**余弦相似度**。

#### 预估用户对候选物品的兴趣分数
```
score(user, item) = Σⱼ like(user, itemⱼ) × sim(itemⱼ, item)
```
其中 itemⱼ 是用户历史交互的物品。

#### 召回完整流程

**离线预计算（建索引）：**
1. **用户 → 物品索引**：记录每个用户最近交互的物品列表，存储为 `(物品ID, 兴趣分数)` 列表
2. **物品 → 物品索引**：计算物品两两相似度，每个物品保存最相似的 k 个，存储为 `(物品ID, 相似度)` 列表

索引的意义：**离线计算量大，线上计算量小**，避免枚举所有物品。

**线上召回：**
1. 给定用户 ID，查"用户→物品"索引，取最近感兴趣的 n=200 个物品（last-n）
2. 对 last-n 中每个物品，查"物品→物品"索引，取 top-k=10 个相似物品
3. 对取回的 nk=2000 个物品，用公式计算用户兴趣分数
4. 返回分数最高的100个物品，作为 ItemCF 通道的输出

---

### 2.2 Swing 模型

> 对应文件：`Slides/02_Retrieval_02.pdf`

#### 动机：ItemCF 的缺陷

ItemCF 用受众重叠度衡量物品相似度，但存在一个问题：

**如果重合的用户来自同一个小圈子**（例如某个微信群集体转发了两篇文章），这两篇文章的受众高度重叠，但它们实际上并不相似（如"某网站护肤品打折" vs "字节裁员了"）。

ItemCF 会误判这两篇文章相似，Swing 通过**降低小圈子用户的权重**来修正这个问题。

#### Swing 的改进

**定义用户重合度**（衡量两个用户是否来自同一小圈子）：
```
overlap(u₁, u₂) = |J₁ ∩ J₂|     （J₁、J₂ 分别为 u₁、u₂ 喜欢的物品集合）
```
两个用户喜欢的物品重合越多 → 越可能来自同一圈子 → 权重越低。

**Swing 物品相似度公式：**
```
sim(i₁, i₂) = Σ_{u₁∈V} Σ_{u₂∈V}  1 / (α + overlap(u₁, u₂))
```
其中：
- V = W₁ ∩ W₂（同时喜欢 i₁ 和 i₂ 的用户集合）
- α 是平滑参数（防止分母为零）
- 两个用户 overlap 越大 → 权重 `1/(α+overlap)` 越小

#### 与 ItemCF 的对比

| | ItemCF | Swing |
|---|--------|-------|
| 物品相似度 | 受众重叠比例 | 受众重叠 + 用户圈子去权 |
| 召回流程 | 相同 | 相同 |
| 优势 | 简单 | 抗小圈子噪声 |

Swing 与 ItemCF **唯一的区别**在于物品相似度的计算，召回流程完全相同。

---

### 2.3 UserCF（基于用户的协同过滤）

> 对应文件：`Slides/02_Retrieval_03.pdf`

#### 核心思想
```
有很多跟我兴趣非常相似的网友
其中某个网友对某笔记点赞、转发    →  推荐这篇笔记给我
我没看过这篇笔记
```

如何找到兴趣相似的用户？
- **方法一**：点击、点赞、收藏、转发的笔记有很大重合
- **方法二**：关注的作者有很大重合

#### 用户相似度公式

设 J_u1、J_u2 分别为两个用户喜欢的物品集合，I = J_u1 ∩ J_u2：

**基础版：**
```
sim(u1, u2) = |I| / sqrt(|J_u1| · |J_u2|)
```

**降低热门物品权重（改进版）：**
```
sim(u1, u2) = Σ_{l∈I} 1/log(1+n_l)
              ─────────────────────────
              sqrt(|J_u1| · |J_u2|)
```
其中 `n_l` 是喜欢物品 l 的用户总数（反映热门程度）。热门物品对很多用户都一样，两人都喜欢热门物品并不说明兴趣相似，因此降权。

#### 预估兴趣分数
```
score(user, item) = Σⱼ sim(user, userⱼ) × like(userⱼ, item)
```
对 top-k 相似用户 userⱼ 加权求和，权重是相似度，like 分数由 userⱼ 对 item 的交互得到。

#### 召回完整流程

**离线预计算（建两个索引）：**
1. **用户 → 物品**：记录每个用户最近交互的 n 个物品 `(物品ID, 兴趣分数)` 列表
2. **用户 → 用户**：对每个用户，索引最相似的 k 个用户 `(用户ID, 相似度)` 列表

**线上召回：**
1. 给定用户 ID，查"用户→用户"索引，找 top-k 相似用户
2. 对每个相似用户，查"用户→物品"索引，取 last-n 个感兴趣物品
3. 对取回的 nk 个物品，用公式计算用户兴趣分数
4. 返回分数最高的 100 个物品

#### UserCF vs ItemCF

| | UserCF | ItemCF |
|---|--------|--------|
| 核心思路 | 找相似用户，推荐其喜欢的物品 | 找相似物品，推荐与历史物品相似的 |
| 索引结构 | 用户→物品 + 用户→用户 | 用户→物品 + 物品→物品 |
| 适合场景 | 社交关系强的平台 | 物品关联强的场景 |

---

### 2.4 离散特征处理

> 对应文件：`Slides/02_Retrieval_04.pdf`

推荐系统中，许多特征是**离散类别**（如性别、国籍、物品ID、用户ID），需要转化为向量。

#### One-Hot 编码

将类别映射为高维稀疏向量：

```
中国 → [1, 0, 0, ...]   (200维)
美国 → [0, 1, 0, ...]
```

**局限**：类别数量很大时（如几亿用户ID）维度爆炸，不可用。

#### Embedding（嵌入）

将序号映射成**低维稠密向量**，通过参数矩阵实现：

```
Embedding = 参数矩阵 × One-Hot向量
```

- **参数数量** = 向量维度 × 类别数量
  - 例：200个国籍，4维向量 → 参数 = 4×200 = 800
  - 例：10000部电影，16维向量 → 参数 = 160,000
- **工程实现**：TensorFlow / PyTorch 提供 Embedding 层，参数以矩阵形式保存，输入序号，输出对应列向量

**类别数量很大时（如物品ID、用户ID），必须用 Embedding 而非 One-Hot。**

---

### 2.5 矩阵补充（Matrix Completion）

> 对应文件：`Slides/02_Retrieval_05.pdf`

矩阵补充是双塔模型的前身（仅用 ID Embedding，不含其他特征）。

#### 模型结构

```
用户ID → Embedding → 向量 a
物品ID → Embedding → 向量 b
预估兴趣 = <a, b>（内积）
```

#### 训练

数据集 Ω = {(用户ID, 物品ID, 兴趣分数)}，兴趣分数由交互行为确定：
- 曝光但未点击 → 0分
- 点击/点赞/收藏/转发 → 各算1分（最高4分）

**优化目标**（最小化均方误差）：
```
min_{A,B} Σ_{(u,i,y)∈Ω} (y - <a_u, b_i>)²
```

#### 线上服务

1. 用户向量 a 存 key-value 表（key=用户ID）
2. 物品向量 b 存向量数据库（Milvus、Faiss、HnswLib）
3. 线上：查用户向量 → 近似最近邻（ANN）查找内积最大的 k 个物品

#### 矩阵补充的三大缺点

| 缺点 | 说明 |
|------|------|
| 仅用 ID Embedding | 没有利用物品类目、关键词、用户性别、年龄等丰富属性 |
| 负样本选取错误 | 用曝光但未点击作为负样本（正确做法应从全体物品采样） |
| 训练方式不佳 | 内积不如余弦相似度；平方损失（回归）不如交叉熵损失（分类） |

**双塔模型是矩阵补充的升级版，解决了这三个缺点。**

---

### 2.6 双塔模型：模型与训练

> 对应文件：`Slides/02_Retrieval_06.pdf`

#### 模型结构

```
用户塔：用户ID + 用户离散特征 + 用户连续特征
        → Embedding + 拼接 → 神经网络 → 用户向量 a

物品塔：物品ID + 物品离散特征 + 物品连续特征
        → Embedding + 拼接 → 神经网络 → 物品向量 b

兴趣预估 = cos(a, b)  （余弦相似度，而非内积）
```

**双塔的关键设计**：用户塔和物品塔**分开**，在神经网络之后才做相似度计算（后期融合）。这使得物品向量可以离线预计算，极大降低线上计算成本。

> 区别：若把用户和物品特征拼接后送入同一神经网络（前期融合），则每次召回都要对所有物品实时计算，代价太大，只适用于粗排/精排，不适合召回。

#### 三种训练方式

**① Pointwise（独立打分）**
- 把召回看做二元分类：正样本鼓励 cos(a,b) → +1，负样本鼓励 cos(a,b) → -1
- 正负样本比例控制为 1:2 或 1:3

**② Pairwise（配对对比）**

每次取一个正样本 b⁺ 和一个负样本 b⁻，鼓励 cos(a,b⁺) > cos(a,b⁻)：

- **Triplet Hinge Loss**：`L = max(0, cos(a,b⁻) + m - cos(a,b⁺))`
- **Triplet Logistic Loss**：`L = log(1 + exp(σ·(cos(a,b⁻) - cos(a,b⁺))))`

**③ Listwise（列表排序）**

一次取一个正样本 + 多个负样本，用 Softmax + CrossEntropy：

```
s_i = softmax([cos(a,b⁺), cos(a,b₁⁻), ..., cos(a,bₙ⁻)])
L = -log(s_+)
```

**Batch 内负样本（常与 Listwise 配合）**：同一 batch 内，每个用户和其他用户对应的物品构成负样本，n个正样本→ n(n-1) 个负样本，实现高效采样。

---

### 2.7 双塔模型：正负样本

> 对应文件：`Slides/02_Retrieval_07.pdf`

#### 正样本

- **定义**：曝光后有点击的 (用户, 物品) 二元组
- **问题**：少部分热门物品占据大多数点击，正样本偏热
- **解决**：过采样（up-sampling）冷门物品，或降采样（down-sampling）热门物品

#### 负样本选择

**简单负样本（来自全体物品）**

未被召回的物品大概率是用户不感兴趣的，因此从全体物品采样：

| 采样方式 | 说明 |
|---------|------|
| 均匀采样 | 对冷门物品不公平（正样本热门，负样本冷门，模型容易） |
| 非均匀采样 | 抽样概率 ∝ 点击次数⁰·⁷⁵，打压热门物品 |

**Batch 内负样本（简单负样本的特殊形式）**

- 物品 j 出现在 batch 内的概率 ∝ 点击次数（比理想的 ∝ 点击次数⁰·⁷⁵ 偏热）
- **纠偏**：训练时把 cos(a,b) 替换为 cos(a,b) - log(p_j)，线上召回时不需要调整

**困难负样本（来自排序阶段被淘汰的物品）**

| 类型   | 来源         | 难度              |
| ---- | ---------- | --------------- |
| 全体物品 | 随机采样       | 简单（easy）        |
| 粗排淘汰 | 被召回但粗排未选中  | 中等（medium hard） |
| 精排靠后 | 被精排打分但排名靠后 | 困难（very hard）   |

**实际训练**：混合使用，50% 简单 + 50% 困难负样本。

#### 常见错误

**曝光但未点击** ≠ 负样本（用于召回时）

- 曝光但未点击的物品，用户可能感兴趣，只是碰巧没点
- 这类样本**可以**作为排序模型的负样本，但**不能**作为召回模型的负样本
- 召回的目标是区分"完全不感兴趣"与"可能感兴趣"

---

### 2.8 双塔模型：线上服务与模型更新

> 对应文件：`Slides/02_Retrieval_08.pdf`

#### 线上召回流程

**离线存储：**
1. 训练完成后，用物品塔计算每个物品的向量 b
2. 将全量物品向量存入向量数据库（Milvus / Faiss / HnswLib）
3. 向量数据库建索引，加速近似最近邻查找

**线上召回：**
1. 给定用户 ID + 用户画像，**实时**用用户塔计算用户向量 a
2. 把 a 作为 query，调用向量数据库做近似最近邻查找
3. 返回余弦相似度最大的 k 个物品

**为什么物品向量离线存储、用户向量线上计算？**
- 每次召回需要 1 个用户向量 vs 数亿个物品向量 → 线上计算物品向量代价过大
- 用户兴趣动态变化 → 用户向量需要实时反映最新兴趣

#### 模型更新策略

**全量更新（每天凌晨）：**
- 以昨天模型参数为基础，用昨天全天数据训练 1 epoch（random shuffle）
- 发布新的用户塔网络 + 更新向量数据库中的物品向量
- 对系统要求低，但延迟高（24小时更新一次）

**增量更新（online learning）：**
- 实时收集线上数据，流式处理生成 TFRecord
- 只更新 **ID Embedding 参数**，锁住全连接层其他参数
- 每隔几十分钟发布最新用户 ID Embedding，供用户塔线上使用

**实际系统：两种方式结合**

| 更新方式 | 更新内容 | 频率 |
|---------|---------|------|
| 全量更新 | 整个神经网络 + 物品向量 | 每天 |
| 增量更新 | 仅 ID Embedding | 每隔几十分钟 |

> **为什么不能只做增量更新？** 小时级数据有偏，分钟级偏差更大。全量更新随机打乱一天数据训练，效果优于按时间顺序的增量训练。

---

### 2.9 双塔模型 + 自监督学习

> 对应文件：`Slides/02_Retrieval_09.pdf`

#### 动机

**头部效应（head effect）**：少部分热门物品占据大多数点击 → 长尾物品点击数据稀少 → 物品塔对长尾物品的向量表征学得差。

**自监督学习**：通过 data augmentation，对同一物品生成两种不同的特征变换，鼓励它们的向量相似，从而更好地学习长尾物品表征。

#### 特征变换方法（数据增强）

| 方法 | 说明 |
|------|------|
| **Random Mask** | 随机遮住部分离散特征（替换为 default 值） |
| **Dropout** | 对多值离散特征随机丢弃 50% 的值 |
| **互补特征（Complementary）** | 把所有特征随机分成两组，各自 mask 另一组 |
| **关联特征 Mask** | 用**互信息（MI）**衡量特征关联度，随机选种子特征并 mask 与其高度相关的一组特征（效果最好，但复杂） |

互信息计算：
```
MI(U, V) = Σ_{u∈U} Σ_{v∈V} p(u,v) · log[p(u,v) / (p(u)·p(v))]
```

#### 训练方式

对同一 batch 的 m 个物品做两类特征变换，得到两组向量：
```
b'_i, b''_i（物品 i 的两种变换表征）
```

**自监督损失**（Listwise，同 batch 内其他物品作为负样本）：
```
L_ssl(i) = -log[ exp(cos(b'_i, b''_i)) / Σⱼ exp(cos(b'_i, b''_j)) ]
```

**总训练损失** = 双塔模型损失 + α × 自监督损失：
```
(1/n)·Σᵢ L_dual(i) + α·(1/m)·Σⱼ L_ssl(j)
```

**实验效果**：长尾物品和新物品的推荐准确率显著提升。

---

### 2.10 Deep Retrieval 召回

> 对应文件：`Slides/02_Retrieval_10.pdf`

#### 与双塔模型的区别

| | 双塔模型 | Deep Retrieval |
|---|---------|---------------|
| 物品表征 | 向量 | 路径（path） |
| 线上查找 | ANN 向量检索 | Beam Search 路径搜索 |

#### 索引结构

将物品表征为树状结构中的路径（如深度=3，宽度=K）：
- 一个物品可对应多条路径（如 `[2,4,1]` 和 `[4,1,1]`）
- **物品 → 路径列表** 的索引
- **路径 → 物品列表** 的索引

#### 预估模型

神经网络按层逐步预估用户对路径的兴趣：
```
p(a|x)       第L1层：给定用户特征x，对节点a的兴趣
p(b|a; x)    第L2层：给定x和a，对节点b的兴趣
p(c|a,b; x)  第L3层：给定x,a,b，对节点c的兴趣

用户对路径[a,b,c]的兴趣 = p(a|x) × p(b|a;x) × p(c|a,b;x)
```

每层将用户特征与前序节点 embedding 拼接后送入神经网络 + Softmax。

#### 线上召回（Beam Search）

1. **Beam Search 找路径**：设 beam size = s，逐层保留分数 top-s 的候选路径
   - size=1（贪心）：每层选最高分节点，效率高但不一定最优
   - size=4：每层保留 4 条候选路径 → L2 计算 4K 个分数选 top-4 → 以此类推
2. **路径 → 物品**：查"路径→物品列表"索引，每条路径取 n 个物品
3. **初步排序**：对 s×n 个物品打分，返回分数最高的子集

#### 训练（同时学习两部分）

**① 神经网络参数学习**

用户点击过物品 → 该物品关联 J 条路径 → 增大这些路径的预估兴趣：
```
loss = -log( Σⱼ p(pathⱼ|x) )   （对J条路径求和）
```

**② 物品表征学习（物品→路径的分配）**

定义物品与路径的相关性：
```
score(item, path) = Σ_{user} p(path|user) × click(user, item)
```

每个物品选 J 条相关性最高的路径，同时添加正则项避免路径负载过大：
```
loss(item, Π) = -log( Σⱼ score(item, pathⱼ) )
reg(pathⱼ) = 该路径上的物品数量
```

用贪心算法逐步更新物品表征。

---

### 2.11 其他召回通道

> 对应文件：`Slides/02_Retrieval_11.pdf`

实际推荐系统有多路并行召回通道，除了协同过滤和双塔，还有：

#### 地理位置召回（无个性化）

- **GeoHash 召回**：对经纬度编码，取用户当前位置附近最新发布的 k 篇优质内容
  - 索引：GeoHash → 优质笔记列表（按时间倒排）
- **同城召回**：取同一城市最新优质内容
  - 索引：城市 → 优质笔记列表

#### 作者召回

| 通道 | 逻辑 | 索引 |
|------|------|------|
| 关注作者召回 | 用户关注的作者 → 其最新笔记 | 用户→关注作者，作者→笔记 |
| 有交互作者召回 | 用户点赞/收藏/转发过的作者 → 其最新笔记 | 用户→有交互作者，作者→笔记 |
| 相似作者召回 | 用户感兴趣的 n 个作者 → 各自相似的 k 个作者 → 最新笔记（共 nk 篇）| 作者→相似作者 |

#### 缓存召回

**背景**：精排输出几百篇笔记，重排只选几十篇，大量精排结果被浪费。

**做法**：缓存精排 top-50 但未曝光的物品，作为新一轮召回的输入。

**缓存退场机制**（满足任一条件即退场）：
- 成功曝光
- 超出缓存容量（FIFO）
- 同一笔记被召回超过 10 次
- 笔记存储超过 3 天

---

### 2.12 曝光过滤与 Bloom Filter

> 对应文件：`Slides/02_Retrieval_12.pdf`

#### 问题

用户看过的物品不应再次曝光。需要对每次召回结果过滤掉已曝光物品。

暴力对比（用户看过 n 个物品，本次召回 r 个）：时间复杂度 O(nr)，线上不可行。

#### Bloom Filter 原理

用 m 维二进制向量表示已曝光物品集合，配合 k 个哈希函数：

**添加物品**：用 k 个哈希函数将物品 ID 映射到向量的 k 个位置，将这 k 位置置为 1。

**查询物品**：检查 k 个位置是否全为 1：
- 有任意位为 0 → **一定不在集合中**（无假阴性）
- k 位全为 1 → **大概率在集合中**（可能假阳性 / 误伤）

```
误伤概率 δ ≈ (1 - exp(-nk/m))^k

最优参数（给定可容忍的 δ）：
k = 1.44 · ln(1/δ)
m = 2n · ln(1/δ)
```

其中 n 是集合中物品数量，n 越大（曝光越多）误伤率越高；m 越大（向量越长）误伤率越低。

#### 工程链路

```
召回结果 → 曝光过滤服务（Bloom Filter）→ 排序
              ↑
实时曝光日志 → 流式处理（Kafka + Flink）→ 写入 Bloom Filter 二进制向量
```

#### Bloom Filter 的局限

- **只支持添加，不支持删除**：一旦某位被置为 1，无法因移除物品而还原
- **需要定期重建**：小红书只召回 1 个月内的笔记，超龄物品需要每天从集合中清除
  - 清除方式：不能直接删除，而是**重建**一个不含超龄物品的新 Bloom Filter

---

> 更新记录：2026-03-26 完成第二章 2.3~2.12 全部内容（UserCF、离散特征处理、矩阵补充、双塔模型三节、自监督学习、Deep Retrieval、其他召回通道、Bloom Filter）

---

## 第三章：精排与粗排

> 对应文件：`Slides/03_Rank_01~06.pdf`

### 3.1 多目标排序模型

> 对应文件：`Slides/03_Rank_01.pdf`

#### 预测目标与融合

精排需要预估多个用户行为分数，再加权融合后排序：

| 指标 | 计算方式 |
|------|---------|
| 点击率（CTR） | 点击次数 / 曝光次数 |
| 点赞率 | 点赞次数 / 点击次数 |
| 收藏率 | 收藏次数 / 点击次数 |
| 转发率 | 转发次数 / 点击次数 |

**融合方式**：对各预估分数做加权和，根据融合分排序截断。

#### 多目标模型结构

```
用户特征 + 物品特征 + 统计特征 + 场景特征
        → Concatenation
        → 共享神经网络（Shared Bottom）
        → 多个独立的全连接层 + Sigmoid
        → 点击率 / 点赞率 / 收藏率 / 转发率
```

**训练**：各目标分别计算交叉熵损失，加权求和作为总损失：

```
CrossEntropy(y, p) = -y·ln(p) - (1-y)·ln(1-p)
总损失 = Σ αₖ · CrossEntropy(yₖ, pₖ)
```

#### 负样本降采样与预估校准

- **问题**：正负样本严重不平衡（如点击率约10%）
- **方案**：负样本降采样（down-sampling），保留比例 α，节约计算资源

降采样后模型的预估点击率偏大，需要用校准公式还原真实点击率：

```
p_true = α · p_pred / (1 - p_pred + α · p_pred)
```

校准之后点击率作为排序的依据

---

### 3.2 MMoE（Multi-gate Mixture-of-Experts）

> 对应文件：`Slides/03_Rank_02.pdf`

#### 基本结构

MMoE 是多目标模型的升级，通过多个"专家"网络和多个"门控"网络，让不同目标共享但又差异化地使用特征：

具体有不有用看各公司尝试之后的结果

```
输入特征
→ n 个专家网络（Expert），各自输出向量 x₁, x₂, ..., xₙ
→ 每个预测目标有独立的门控（Gate）：
    门控 = Softmax → 权重向量 (p₁, ..., pₙ)
    加权输出 = p₁·x₁ + p₂·x₂ + ... + pₙ·xₙ
→ 各目标独立的上层网络 → 预估分数
```

多个目标对应多个独立的门控网络，使得各目标可以有差异地选择专家。

#### 极化问题（Polarization）

- **问题**：训练时 Softmax 输出趋向极端（一个接近1，其余接近0），某些专家的权重长期接近 0 → 该专家"死亡"（参数得不到有效更新）
- **解决方案**：对 Softmax 输出使用 Dropout（每个专家的权重以 10% 概率被 mask 为 0），强制各专家在训练时都被使用
  - dropout基本能解决极化问题

---

### 3.3 预估分数的融合

> 对应文件：`Slides/03_Rank_03.pdf`

实际系统中，不同平台有不同的融分公式：

**简单加权和：**
```
score = p_click + w₁·p_like + w₂·p_collect + ...
```

**点击率乘以其他指标（更常见）：**
```
score = p_click · (1 + w₁·p_like + w₂·p_collect + ...)
```

> 直觉：点击是前提，只有点击后才会产生点赞/收藏行为，因此用点击率做乘法。

**海外某短视频 App：**
```
score = (1 + w₁·p_like)^a · (1 + w₂·p_comment)^b · ...
```

**国内某短视频 App（基于排名的融合）：**

对每个目标（时长、点赞、转发等）分别排名，得到各自排名 r，再加权：
```
score = w₁/(r_duration + β) + w₂/(r_like + β) + w₃/(r_share + β) + ...
```

**某电商平台（链式转化概率）：**
```
score = p_click^α × p_cart^β × p_pay^γ × price^δ
```

---

### 3.4 视频播放建模

> 对应文件：`Slides/03_Rank_04.pdf`

视频特有的排序信号：**播放时长**和**完播率**。

#### 播放时长建模（YouTube 方法）

直接用回归预测时长效果不好，YouTube 用一个技巧：把时长建模转化为加权的二元分类问题。

- 设模型最后一层输出 z，令 p = sigmoid(z)
  - 用作训练
- 实际观测播放时长为 t（未点击则 t=0）
  - 定义y = t / (1+t)，范围在 [0,1)，作为训练标签
- **训练**：最小化加权交叉熵损失：

```
loss = -t/(1+t) · log(p) - 1/(1+t) · log(1-p)
```

- **推理**：用 exp(z) 作为播放时长的预估值（因为 p=y 时 exp(z)=t）；也作为融分公式中的一项

#### 完播率建模

**回归方法：**
- 计算实际播放率 y = 实际播放时长 / 视频总时长
- 最小化交叉熵：`loss = y·log(p) + (1-y)·log(1-p)`
- 线上预估完播率 p，如 p=0.73 表示预计播放 73%

**二元分类方法：**
- 定义完播阈值（如完播 80%），构造正负样本
- 线上预估 ℙ(完播>80%)

**注意**：不能直接把预估完播率用于融分，因为完播率与视频时长有强相关性（短视频更容易完播）。需要除以视频时长的校准函数 f(视频长度)：
- 视频越长，f值越小
```
p_adjusted = p_estimated / f(视频长度)
```

---

### 3.5 排序模型的特征

> 对应文件：`Slides/03_Rank_05.pdf`

精排模型使用四大类特征：

#### 1. 用户画像（User Profile）
- 用户 ID（做 Embedding）
  - 通常32或者64维
- 人口统计学：性别、年龄
- 账号信息：新/老用户、活跃度
- 感兴趣的类目、关键词、品牌

#### 2. 物品画像（Item Profile）
- 物品 ID（做 Embedding）
  - 非常重要的特征
- 发布时间
- GeoHash（经纬度编码）、城市
- 标题、类目、关键词、品牌
  - 做embedding变成向量
- 字数、图片数、视频清晰度、标签数
- 内容信息量、图片美学质量

#### 3. 用户统计特征
- 最近 30天/7天/1天/1小时 的曝光数、点击数、点赞数、收藏数
- 按笔记类型分桶（图文 vs 视频点击率）
- 按笔记类目分桶（美妆/美食/科技数码……的点击率）

#### 4. 笔记统计特征
- 笔记各时间窗口的曝光、点击、互动数
  - 比如笔记最近的曝光、点击、互动数显著变低，说明笔记已经过时
- 按用户性别/年龄分桶
- 作者特征：发帖数、粉丝数、消费指标

#### 5. 场景特征（Context）
- 用户定位 GeoHash、城市
- 当前时刻（分段做 Embedding）
- 是否是周末/节假日
- 手机品牌、型号、操作系统

#### 特征处理

| 特征类型 | 处理方式 |
|---------|---------|
| 离散特征（用户ID、笔记ID、类目等） | 做 Embedding |
| 连续特征（年龄、字数、视频长度） | 分桶 → 离散 → Embedding |
| 计数特征（曝光数、点赞数等） | 取 log(1+x)；或转化为点击率/点赞率并做平滑 |

离散特征做embedding消耗内存最大，连续特征稍微好一些

**特征覆盖率**：很多特征无法覆盖 100% 样本（如用户不填年龄、隐私限制地理定位），提高覆盖率可提升精排准确性。

要考虑当特征缺失的时候用什么作为默认值。
- 用户画像：较为静态
  - 数据库压力大
- 物品画像：静态
  - 数据库压力小
- 统计数据：动态

---

### 3.6 粗排：三塔模型

> 对应文件：`Slides/03_Rank_06.pdf`

#### 粗排 vs 精排

| | 粗排 | 精排 |
|--|------|------|
| 候选规模 | 数千 | 数百 |
| 单次推理代价 | 必须小 | 可以大 |
| 准确性 | 较低 | 较高 |

- **精排（前期融合）**：把所有特征 Concatenation 后送入大模型，线上需对每个候选物品做完整推理，代价为 O(n)
- **双塔模型（后期融合）**：用户塔/物品塔分离，物品向量可离线存储，线上只需用户塔推理1次

双塔精度不如精排，粗排需要在两者之间取得平衡。

#### 三塔模型（COLD，阿里）

```
用户塔（很大）  物品塔（较大）  交叉塔（较小）
 ↓                ↓              ↓
用户特征       物品特征(静态)   统计特征+交叉特征
                               +场景特征
         Concatenation & Cross
         ↓
  多目标打分（点击率、点赞率……）
```

**各塔推理策略：**

| 塔 | 推理次数 | 优化方式 |
|----|---------|---------|
| 用户塔 | 只做 **1次**（只有1个用户） | 无需缓存，直接计算 |
| 物品塔 | 理论 n 次，实际极少 | **PS 缓存**物品塔输出向量 |
| 交叉塔 | **必须 n 次**（统计特征动态变化，无法缓存） | 控制模型较小 |

> 有n个物品，上层网络也需做 n 次推理，粗排的大部分计算量在上层网络（和交叉塔）。
>  - 上层网络定义回顾：多目标模型中，交叉塔输出与统计特征、场景特征拼接后送入上层网络，输出点击率/点赞率等预估分数。

---

## 第四章：特征交叉

> 对应文件：`Slides/04_Cross_01~04.pdf`

### 4.1 Factorized Machine（FM）
现在已经不再常用了，可以了解
> 对应文件：`Slides/04_Cross_01.pdf`

#### 线性模型的局限

线性模型只能做特征的加权和，无法捕捉特征间的交互：（特征之间没有交叉）
```
p = b + Σᵢ wᵢxᵢ
```

#### 引入二阶交叉特征

加入所有特征对的乘积项：
```
p = b + Σᵢ wᵢxᵢ + Σᵢ Σⱼ>ᵢ uᵢⱼ · xᵢxⱼ
```

问题：参数矩阵 U 有 O(d²) 个参数，d 很大时参数量爆炸且数据稀疏导致难以训练。容易出现overfitting。

#### FM 的核心思想：矩阵分解

将 U 近似分解为低秩矩阵：**U ≈ V·Vᵀ**，即 uᵢⱼ ≈ vᵢᵀvⱼ（内积）

**FM 公式：**
```
p = b + Σᵢ wᵢxᵢ + Σᵢ Σⱼ>ᵢ <vᵢ, vⱼ> · xᵢxⱼ
```

- 参数量从 O(d²) 降低到 **O(kd)**（k ≪ d，k 是 Embedding 维度）
- FM 是线性模型的替代品，任何用线性回归/逻辑回归的场景都可用 FM
- FM 使用二阶交叉特征，表达能力比线性模型更强，但这个方法其实还是过时了

---

### 4.2 深度交叉网络（DCN）

> 对应文件：`Slides/04_Cross_02.pdf`

专家神经网络可以用任意结构，比如这里的DCN和之后的其他网络。

#### 交叉层（Cross Layer）

交叉层通过逐元素乘法（Hadamard Product）实现显式的高阶特征交叉：

```
x_{l+1} = x₀ ∘ (W_l · x_l + b_l) + x_l
```

- x₀：原始输入（残差连接来源）
- x_l：第 l 层的输入
- W_l, b_l：该层的可学习参数
- ∘：Hadamard 乘积（逐元素乘）

交叉层的输入分为两部分，一部分是原始输入 x₀，另一部分是上一层的输出 x_l。通过 W_l · x_l + b_l 计算交叉特征，再与 x₀ 做逐元素乘法，最后加上 x_l 的残差连接。

#### 交叉网络（Cross Network）

将多个交叉层串联，每层都与原始输入做交叉，实现更高阶的特征组合：
```
x₁ = x₀ ∘ (W₀·x₀ + b₀) + x₀
x₂ = x₀ ∘ (W₁·x₁ + b₁) + x₁
x₃ = x₀ ∘ (W₂·x₂ + b₂) + x₂
```

#### 深度交叉网络（Deep & Cross Network，DCN）

将交叉网络与全连接网络（DNN）并行，再拼接输出：

```
用户/物品/其他特征
    ├── 交叉网络（Cross Network）→ 显式高阶交叉特征
    └── 全连接网络（DNN）→ 隐式特征组合
        ↓
    Concatenation → 全连接层 → 预测分数
```

DCN 可作为多目标模型、双塔模型、MMoE 等结构的 **backbone**，替换其中的 shared bottom 神经网络。可用于召回，也可用于排序。

---

### 4.3 LHUC 网络结构

> 对应文件：`Slides/04_Cross_03.pdf`

#### 来源

LHUC（Learning Hidden Unit Contributions）起源于**语音识别**中的说话人自适应，后被快手引入推荐精排（称为 **PPNet**）。

#### 核心思想

在主干网络的每个全连接层之后，用辅助输入（语音识别中为说话人特征，推荐中为**用户特征**）生成一组门控权重，对主干网络的输出做逐元素缩放（Hadamard Product），实现对不同用户的个性化适配。

#### 在推荐精排中的应用

```
物品特征 → 全连接层 → Hadamard Product → 全连接层 → Hadamard Product → 输出
              ↑                                ↑
用户特征 → 神经网络 → 2·Sigmoid          → 神经网络 → 2·Sigmoid
            （门控网络，输出范围 [0, 2]）
```

- 主路：物品特征经过全连接层
- 侧路：用户特征经过小型神经网络 + Sigmoid×2（输出范围 [0,2]，1为中性）
- 逐元素相乘：根据用户特征动态调整物品特征在每个维度的权重

**作用**：让模型根据当前用户身份，对物品特征的每个维度做动态加权，实现用户个性化的特征交叉，而不需要将所有特征强制 concatenation。

---

### 4.4 SENet 与 Bilinear Cross（FiBiNET）

> 对应文件：`Slides/04_Cross_04.pdf`

#### SENet（Squeeze-and-Excitation Network）

SENet 原用于图像识别，被引入推荐系统用于对离散特征做 **field 级别的动态加权**。

**处理流程：**

```
m 个离散特征（各自 Embedding）
→ 拼成矩阵 (m × k)
→ AvgPool：对每个 field 的 Embedding 求均值 → 得到 (m × 1) 向量
→ FC + ReLU → FC + Sigmoid：输出 m 维权重向量
→ row-wise multiply：对每个 field 的 Embedding 乘以对应权重
→ 输出重加权后的 (m × k) 矩阵
```

- **Field-wise 加权**：同一 field（如"用户ID"的 64 维向量）的所有元素共享同一个权重
- 让模型自动学习哪些特征域更重要

#### Field 间特征交叉

两个 field 向量 xᵢ 和 xⱼ 之间有三种交叉方式：

| 方法 | 公式 | 输出 |
|------|------|------|
| 内积（Dot Product） | fᵢⱼ = xᵢᵀxⱼ | 1个实数 |
| 哈达玛乘积 | fᵢⱼ = xᵢ ∘ xⱼ | k维向量 |
| **Bilinear Cross（内积版）** | fᵢⱼ = xᵢᵀ · Wᵢⱼ · xⱼ | 1个实数 |
| **Bilinear Cross（哈达玛版）** | fᵢⱼ = (xᵢ · Wᵢⱼ) ∘ xⱼ | k维向量 |

Bilinear Cross 引入可学习的参数矩阵 Wᵢⱼ，比直接内积/哈达玛乘积表达力更强。

实践中，一般人工指定一部分向量进行交叉，可以减少参数的数量，也可以让concatenation之后的向量纬度减小。

#### FiBiNET（Feature Importance and Bilinear feature Interaction Network）

将 SENet 和 Bilinear Cross 结合：

```
离散特征 Embedding (m × k)
    ├── 直接 Bilinear Cross → 交叉特征向量
    └── 经过 SENet 重加权 → Bilinear Cross → 交叉特征向量
        ↓
Concatenate（两组交叉特征 + 连续特征）
        ↓
上层网络 → 预测分数
```

- SENet 分支：先用 SENet 对 field 重要性加权，再做 Bilinear Cross，捕捉经过筛选后的特征交互
- 原始分支：不经 SENet，直接做 Bilinear Cross

---

> 更新记录：2026-03-27 完成第三章（多目标排序、MMoE、融分公式、视频播放建模、精排特征、粗排三塔）和第四章（FM、DCN、LHUC/PPNet、SENet/FiBiNET）

---

## 第五章：用户行为序列建模

> 对应文件：`Slides/05_LastN_01~03.pdf`

### 5.1 LastN 特征

> 对应文件：`Slides/05_LastN_01.pdf`

#### 基本思想

用户的近期行为（点击、点赞、收藏等）反映了其当前兴趣。**LastN 特征**是指用户最近 n 次交互物品的 ID。

**处理方式：**
```
用户最近 n 个交互物品 ID
→ 各自做 Embedding → n 个向量
→ 取平均 → 1 个向量
→ 作为用户特征输入模型
```

**来源**：YouTube 推荐论文（Covington et al., RecSys 2016）。

**适用范围**：召回双塔模型、粗排三塔模型、精排模型均适用。

#### 小红书的实践

不止记录点击，而是分别对**点击、点赞、收藏**各自维护 LastN 列表，分别做 Embedding + 平均，得到多个用户兴趣向量后一起输入模型。

---

### 5.2 DIN 模型（Deep Interest Network）

> 对应文件：`Slides/05_LastN_02.pdf`

#### 核心思想

LastN 简单平均对所有历史物品一视同仁，但用户兴趣是多样的：推荐美食时，历史中美食相关的交互应赋予更高权重；推荐汽车时，汽车相关的交互应赋予更高权重。

**DIN 用加权平均代替简单平均**，以候选物品为 query，对 LastN 物品向量做注意力加权：

```
LastN 物品向量：x₁, x₂, ..., xₙ
候选物品向量：q

相似度权重：αᵢ = similarity(xᵢ, q)

加权和：Σᵢ αᵢ · xᵢ  → 作为用户特征输入精排模型
```

#### 本质：单头注意力机制

- LastN 向量同时作为 **key** 和 **value**
- 候选物品向量作为 **query**
- 输出是 value 的加权和（attention 的标准形式）

**参考文献**：Zhou et al., Deep Interest Network for Click-Through Rate Prediction, KDD 2018.

#### 为什么有效

用户兴趣是多元的，简单平均后兴趣信号互相稀释。对于某个具体候选物品，注意力机制能够聚焦于历史中与其相关的行为，让用户特征表示更有针对性。

#### 使用限制

| 模型 | 是否可用 DIN | 原因 |
|------|-------------|------|
| 精排模型 | ✅ | 可同时看到 LastN 和候选物品 |
| 双塔/三塔模型 | ❌ | 用户塔看不到候选物品，无法计算注意力权重 |

双塔和三塔模型只能使用**简单平均**的 LastN 特征（用户塔的输入不含候选物品信息）。

---

### 5.3 SIM 模型（Search-based Interest Model）

> 对应文件：`Slides/05_LastN_03.pdf`

#### DIN 的局限

DIN 注意力层的计算量 ∝ 序列长度 n，因此只能记录最近几百个交互物品。**缺点：只关注短期兴趣，遗忘了长期兴趣。**

#### SIM 的改进思路

用户的长期行为序列（几千条）中，与某个候选物品相关的只是很小一部分。**先做快速过滤，再做注意力计算**：

```
用户长期行为序列（n 条，n 可达几千）
↓ 快速查找：找到 top-k 个与候选物品相似的历史物品（k ≪ n）
↓ 注意力层（只对 k 条记录计算）
↓ 输出用户行为特征 → 精排模型
```

计算量从 O(n) 降至 O(k)。

**参考文献**：Qi et al., Search-based User Interest Modeling with Lifelong Sequential Behavior Data for Click-Through Rate Prediction, CIKM 2020.

#### 第一步：快速查找（两种方法）

**方法一：Hard Search**
- 保留 LastN 中与候选物品**类目相同**的物品
- 简单、快速、无需额外训练

**方法二：Soft Search**
- 对物品做 Embedding，将候选物品向量作为 query
- 在 LastN 的物品向量中做 **k 近邻查找**，取最相似的 k 个
- 效果更好，工程实现更复杂

实际选择取决于工程基建能力。

#### 第二步：注意力机制（加入时间信息）

SIM 序列更长，记录了用户长期兴趣，时间越久远的交互重要性越低，因此引入**时间 Embedding**：

```
每个 LastN 物品的表征 = concat(物品 Embedding x, 时间 Embedding d)
```

- 用户与该物品的交互距今时间 δ → 离散化 → Embedding → 向量 d
- 拼接后得到携带时间信息的物品表征
- 再输入注意力层与候选物品做加权

DIN 序列短（近期行为），时间差异不大，无需时间信息；SIM 序列长（长期行为），时间衰减对效果有明显提升。

#### 结论

| 方法 | 效果 |
|------|------|
| 长序列（长期兴趣）vs 短序列 | 长序列更优 |
| 注意力机制 vs 简单平均 | 注意力机制更优 |
| Soft Search vs Hard Search | 取决于工程基建 |
| 加入时间信息 | 有提升 |

---

> 更新记录：2026-03-27 完成第五章（LastN 特征、DIN 注意力机制、SIM 长序列建模）

---

## 第六章：重排与多样性

> 对应文件：`Slides/06_Rerank_01~05.pdf`

### 6.1 多样性概述与物品相似度度量

> 对应文件：`Slides/06_Rerank_01.pdf`

精排用多目标模型对每个物品单独打分（pointwise），只考虑物品本身的价值，不考虑物品之间的关系。**重排**在精排之后，从 n 个候选物品中选出 k 个，在保持高分的同时兼顾**多样性**（避免同质化内容扎堆）。

#### 物品相似度的三种度量

| 方法 | 说明 | 优劣 |
|------|------|------|
| **属性标签** | 根据一级/二级类目、品牌等字段计算重合度 | 简单快速，但粒度粗 |
| **双塔模型物品向量** | 用召回双塔的物品 Embedding 计算余弦相似度 | 反映用户兴趣相似性，不够好 |
| **内容向量（CLIP）** | 用图文多模态预训练模型提取内容向量 | 效果最好 |

- 双塔模型训练不好长尾和新物品的向量，导致相似度不准确

**CLIP 训练方式**：对图片-文字二元组预测是否匹配（Contrastive Learning），无需人工标注。一个 batch 内 m 对正样本 + m(m-1) 对负样本。小红书笔记天然包含图片+文字，非常适合用 CLIP。

#### 推荐系统的链路：
```
召回结果 → 粗排 → 后处理 → 精排 → 后处理 → 物品1
          （也需要多样性算法）   （重排）  物品2
                                      ...
                                      物品k
```
- 粗排和精排用多目标模型对物品做pointwise打分
- 对于物品i，模型输出点击率、交互率的评估，融合成分数 $reward_i$
- $reward_i$ 表示用户对物品i的兴趣，即物品本身价值
- 后处理：给定n个候选物品，选出k个物品，要求这k个物品**分数高**（相关性）且**不相似**（多样性）
---

### 6.2 MMR 多样性算法

> 对应文件：`Slides/06_Rerank_02.pdf`

#### 核心思想

**Maximal Marginal Relevance（MMR）**：每次从未选集合 ℛ 中选出一个物品，使其 **精排分数高** 且 **与已选集合 𝒮 中的物品不相似**：

```
MRᵢ = θ · rewardᵢ - (1-θ) · max_{j∈𝒮} sim(i, j)
MMR = argmax_{i∈ℛ} MRᵢ
```

- `reward_i`：精排分数（物品价值）
- `max_{j∈𝒮} sim(i, j)`：物品 i 与已选集合中最相似物品的相似度（多样性惩罚）
- θ：控制相关性与多样性的权衡

#### MMR 算法流程

1. 𝒮 初始化为空集，ℛ 初始化为全部 n 个候选物品
2. 选出精排分数最高的物品，从 ℛ 移入 𝒮
3. 重复 k-1 次：计算 ℛ 中所有物品的 MR 分数，选出最高的移入 𝒮

#### 滑动窗口优化

随着 𝒮 不断增大，其中物品越来越多，很难再找到与所有已选物品都不相似的新物品，导致 `max sim(i,j)` 总是接近 1，MMR 退化。

**解决方案**：用**滑动窗口 𝒲**（如最近选中的 10 个物品）替代 MMR 公式中的 𝒮：

```
MRᵢ = θ · rewardᵢ - (1-θ) · max_{j∈𝒲} sim(i, j)
```
- 物品最好与最近推送的物品不相似，但第30个物品没必要和第一个物品不相似。

---

### 6.3 重排规则

> 对应文件：`Slides/06_Rerank_03.pdf`

除多样性算法外，重排还需满足各类**业务规则**。以小红书为例（注：非真实数据）：

| 规则类型 | 示例 |
|---------|------|
| **最多连续 k 篇同类** | 最多连续 5 篇图文笔记，最多连续 5 篇视频笔记 |
| **每 k 篇最多出现 1 篇某类** | 每 9 篇笔记最多出现 1 篇运营推广笔记 |
| **前 t 篇最多出现 k 篇某类** | 首屏（前 4 篇）最多 1 篇电商卡片笔记 |

#### MMR + 规则结合

每轮 MMR 选物品前，先用规则从 ℛ 中排除掉不符合约束的物品，得到子集 ℛ'，再在 ℛ' 上执行 MMR：

```
argmax_{i∈ℛ'} θ · rewardᵢ - (1-θ) · max_{j∈𝒲} sim(i, j)
```

---

### 6.4 DPP：数学基础

> 对应文件：`Slides/06_Rerank_04.pdf`

#### 超平行体与体积

k 个向量 v₁, ..., vₖ ∈ ℝᵈ 可以张成一个 k 维**超平行体**（2维→平行四边形，3维→平行六面体）：

```
𝒫(v₁, ..., vₖ) = { α₁v₁ + ... + αₖvₖ | 0 ≤ α₁,...,αₖ ≤ 1 }
```

体积反映向量的**分散程度**：
- 向量**两两正交**（多样性好）→ 体积最大化，= 1（单位向量时）
- 向量**线性相关**（多样性差）→ 体积 = 0

#### 用行列式计算体积

将 k 个物品向量排列为矩阵 V（d×k），则：

```
det(VᵀV) = vol²(𝒫(v₁,...,vₖ))
```

因此 det(VᵀV) 可以作为物品集合多样性的衡量指标。

---

### 6.5 DPP 多样性算法

> 对应文件：`Slides/06_Rerank_05.pdf`

#### 目标函数

**行列式点过程（Determinantal Point Process, DPP）** 将精排分数与多样性统一到一个优化目标：

```
argmax_{𝒮: |𝒮|=k}  θ · Σ_{i∈𝒮} rewardᵢ  +  (1-θ) · log det(A_𝒮)
```

其中 A 是 n×n 的物品相似度矩阵（aᵢⱼ = vᵢᵀvⱼ），A_𝒮 是 𝒮 对应的 k×k 子矩阵。

#### 贪心求解

每轮从未选集合 ℛ 中选一个物品加入 𝒮，使目标最大化：

```
argmax_{i∈ℛ} θ · rewardᵢ + (1-θ) · log det(A_{𝒮∪i})
```
- 对于单个i，计算A_{𝒮∪i}的行列式需要 O(k³) 的时间复杂度（最大为k×k矩阵的行列式），每轮需要对 ℛ 中所有物品计算一次，整体复杂度为 O(nk³)；需要求解上式k次才能选出k个物品，如果暴力计算，总时间复杂度为 O(nk⁴)
- 计算矩阵A的时间复杂度是 O(n²d)，因为需要计算 n(n-1)/2 个物品对的相似度，每个相似度计算 O(d)

**朴素算法复杂度**：O(n²d + nk⁴)

#### Hulu 快速算法（Cholesky 分解）

- 利用 Cholesky 分解 A_𝒮 = LLᵀ（L 为下三角矩阵）
- 下三角矩阵的行列式 = 对角线元素乘积：det(L²) = (∏ lⱼⱼ)²
- 已知上一轮的 Cholesky 分解，可以快速更新 A_{𝒮∪i} 的分解

**优化后复杂度**：O(nd + nk²)，比朴素算法快得多

#### 滑动窗口与规则约束

与 MMR 相同：
- 用**滑动窗口 𝒲** 替代 𝒮，避免行列式坍缩到零
- 每轮用**规则**过滤 ℛ 得到子集 ℛ'，再在 ℛ' 上做 DPP 贪心选择

---

## 第七章：物品冷启动

> 对应文件：`Slides/07_ColdStart_01~06.pdf`

### 7.1 冷启动评价指标

> 对应文件：`Slides/07_ColdStart_01.pdf`

- UGC: User Generated Content，用户生成内容
- PGC: Professionally Generated Content，专业生产内容

**物品冷启动**：平台上用户新发布的内容（UGC），缺少用户交互数据，推荐难度大。冷启动的三个目标：
1. **精准推荐**：把新内容推给合适用户
2. **激励发布**：让作者获得早期正反馈，提升创作积极性
3. **挖掘高潜**：通过小流量试探，识别优质内容给予扶持

#### 三类评价指标

**作者侧指标**
- **发布渗透率** = 当日发布人数 / 日活人数
  - 例：日活2000万，当日发布人数20万，发布渗透率=1%
- **人均发布量** = 当日发布笔记数 / 日活人数
  - 例：日活2000万，当日发布笔记数40万，人均发布量=0.02
- 反映出作者的发布积极性

**用户侧指标**
- 新笔记的**点击率、交互率**（需区分高曝光/低曝光分开看，因为曝光基尼系数大）
- 大盘**消费时长、日活、月活**（过度扶持新笔记会损害大盘消费体验，两者需要平衡）
  - 大力扶持低曝光笔记会让作者侧发布指标变好，但用户侧大盘消费指标变差

**内容侧指标**
- **高热笔记占比**：前 30 天获得 1000+ 次点击的笔记比例，衡量冷启阶段挖掘优质内容的能力

作者/用户侧是基本大厂都会用的，内容侧少数几家大厂会用。

---

### 7.2 冷启动召回：基础方法

> 对应文件：`Slides/07_ColdStart_02.pdf`

#### 冷启召回的困难

新笔记缺少用户交互：
- **ItemCF 不适用**：需要用户交互数据构建物品相似度，新笔记没有
- **双塔模型效果差**：笔记 ID Embedding 还未学好（没有足够交互训练）
  - 双塔模型是召回中最重要的模型
  - ID最重要的特征之一，不仅在召回中，也在排序中

#### 双塔模型改造（两种方案）

**方案一：Default Embedding**
- 所有新笔记共享一个默认 ID，使用同一个 embedding 向量（经过学习的默认向量）
- 等到下次全量模型训练后，新笔记才有自己专属的 ID Embedding

**方案二：借用相似笔记 Embedding**
- 找 top-k 内容最相似的高曝光笔记（高曝光笔记的 ID Embedding 训练得比较好）
- 将 k 个高曝笔记的 Embedding 取平均，作为新笔记的 Embedding

**多召回池**：维护 1小时、6小时、24小时、30天等多个新笔记召回池，共享同一个双塔模型（不增加训练代价），让新笔记有更多曝光机会。

#### 类目/关键词召回

- 维护索引：`类目 → 笔记列表（按时间倒排）`、`关键词 → 笔记列表（按时间倒排）`
- 线上：根据用户画像中的感兴趣类目/关键词，取最新的 k 篇笔记

**缺点**：只对刚发布的新笔记有效（几小时后即不再被召回）；个性化程度低，精准性不足。
**优点**：能够使新发布的笔记立即被召回。

---

### 7.3 冷启动召回：聚类召回

> 对应文件：`Slides/07_ColdStart_03.pdf`

#### 核心思想

基于内容相似度：用户喜欢某篇笔记，则会喜欢内容相似的笔记。

#### 内容相似度模型

```
笔记 = 图片 + 文字
→ CNN（图片特征）+ BERT（文字特征）→ 全连接层 → 笔记特征向量
```

**训练**（Triplet Loss）：

```
正样本：内容相似的高曝光笔记对（同二级类目 + ItemCF相似度高）
负样本：从全体随机采样（字数多、质量高的笔记）

Triplet Hinge Loss: L = max(0, cos(a,b⁻) + m - cos(a,b⁺))
Triplet Logistic Loss: L = log(1 + exp(cos(a,b⁻) - cos(a,b⁺)))
```

#### 聚类索引

- 用 k-means（余弦相似度）对全量笔记向量聚类，划分为约 1000 个 cluster
- 新笔记发布后，用神经网络提取向量，找到最近 cluster，加入其笔记列表
- 索引：`cluster → 笔记 ID 列表（按时间倒排）`

#### 线上召回流程

```
用户 last-n 交互笔记（种子笔记）
→ 映射为特征向量
→ 找到最近的 cluster
→ 从 cluster 的笔记列表中取最新的 m 篇
→ 最多取回 mn 篇新笔记
```

---

### 7.4 冷启动召回：Look-Alike 人群扩散

> 对应文件：`Slides/07_ColdStart_04.pdf`

#### 思想来源

Look-Alike 起源于互联网广告：先找到产品的"种子用户"（典型目标受众），再找与种子用户相似的扩散人群，定向投放广告。

#### 用于新笔记召回

- **种子用户**：对新笔记有点击/点赞/收藏/转发行为的用户
- **笔记特征向量**：种子用户向量的平均值（**近线更新**：每当有新交互，重新计算平均）
  - 近线的意思是不用做到秒级更新，几分钟更新一次即可
- **线上召回**：将用户向量存入向量数据库，给定笔记特征向量做 k 近邻查找，找到相似用户

```
新笔记 → 累计种子用户行为 → 更新笔记特征向量（种子用户向量均值）
                                    ↓
                        在用户向量数据库中做近邻查找
                                    ↓
                           找到相似用户 → 向其推荐该笔记
```

---

### 7.5 流量调控

> 对应文件：`Slides/07_ColdStart_05.pdf`

#### 扶持新笔记目的：
- 促进发布，增大内容池：
  - 新笔记获得的曝光越多，作者创作积极性越高；反映在发布渗透率、人均发布量
- 挖掘优质笔记：
  - 做探索，让每篇新笔记都能获得足够曝光；挖掘的能力反映在高热笔记占比

冷启动需要主动给新笔记倾斜流量，工业界演进路径：

#### 1. 新笔记提权（Boost）

在粗排、重排阶段给新笔记的分数乘以大于 1 的系数（boost），使新笔记有更多机会排名靠前获得曝光。

**缺点**：曝光量对提权系数非常敏感，很难精确控制，容易过度曝光或曝光不足。

> 注意：给新笔记分数 boost 越多并非越好。过度 boost 会把笔记推给不合适的受众，导致点击率、点赞率偏低，长期反而被推荐系统打压。

#### 2. 新笔记保量

**保量**：保证每篇新笔记在发布后 24 小时内至少获得 100 次曝光。

**动态提权**：根据"发布时间 / 目标时间"和"已有曝光 / 目标曝光"动态调整提权系数：

```
提权系数 = f(发布时间/目标时间, 已有曝光/目标曝光)
```

发布时间越长但曝光越少 → 提权系数越高。

**保量难点**：
- 保量成功率远低于 100%（召回、排序不足，或提权系数调得不好）
- 线上环境变化（新增召回通道、升级排序模型等）会导致保量失败，需要重新调整系数

#### 3. 差异化保量

不同质量的笔记保不同的量：

| 笔记类型 | 保量目标 |
|---------|---------|
| 普通笔记 | 基础 100 次曝光 |
| 内容优质 | +最多 200 次额外曝光 |
| 作者质量高 | +最多 200 次额外曝光 |
| 上限 | 500 次曝光 |

（注：非小红书真实数据）

---

### 7.6 冷启动的 AB 测试

> 对应文件：`Slides/07_ColdStart_06.pdf`

冷启动 AB 测试的特殊性：需要**同时观测作者侧发布指标和用户侧消费指标**，两者存在天然矛盾。

#### 用户侧实验的问题

标准用户侧实验（50% 实验组、50% 对照组，共享全量笔记池）：
- 实验组用户看到更多新笔记 → 消费指标变差
- 对照组用户看到更少新笔记 → 消费指标变好
- 两组 diff 被放大，**实验 diff 远大于推全后的真实 diff**

#### 作者侧实验的三种方案

**方案一：笔记按作者分桶**
- 50% 作者（实验组）的新笔记应用新策略，其余作者对照
- 所有用户共享全量笔记池
- **缺点**：实验组新笔记和对照组新笔记在同一用户池中**抢流量**，AB 测试 diff 与推全结果有偏差

**方案二：笔记和用户同时分桶**
- 新笔记按作者分桶（50% 实验 / 50% 对照）
- 用户也分桶（50% 实验 / 50% 对照），各看各自桶的笔记
- **优点**：实验组新笔记的两个桶不抢流量，作者侧结果更可信
- **缺点**：新笔记池缩小一半，对用户体验有负面影响

**方案三：笔记、用户、老笔记三维分桶**
- 笔记（新）、用户、老笔记各自 50% 分桶，共 8 个组合
- 能更精确地隔离各种流量竞争效应
- **缺点**：会严重损害用户体验

**设计 AB 方案时需要考虑的关键问题：**
1. 实验组、对照组新笔记之间会不会抢流量？
2. 新笔记和老笔记怎么竞争流量？
3. 同时隔离笔记和用户，会不会让内容池变小？
4. 如果对新笔记做保量，实验结果会怎样变化？

---

> 更新记录：2026-03-27 完成第六章（多样性度量、MMR、重排规则、DPP 数学基础与算法）和第七章（冷启动指标、召回方法四种、流量调控与保量、AB测试方案）

---

## 第八章：涨指标的方法

> 对应文件：`Slides/08_Improvement_01~06.pdf`

### 8.1 推荐系统的核心指标与涨指标方向

> 对应文件：`Slides/08_Improvement_01.pdf`

#### 指标体系

| 级别 | 指标 | 说明 |
|------|------|------|
| **最核心** | DAU、留存（LT7/LT30） | LT7：今天登录后未来 7 天中的登录天数，1≤LT7≤7 |
| **核心** | 使用时长、总阅读数、总曝光数 | 时长↑ → 留存通常↑；时长↑ → 阅读/曝光可能↓ |
| **非核心** | 点击率、交互率 | 用于模型训练，不直接优化 |
| **UGC 平台额外** | 发布量、发布渗透率 | 反映内容供给侧健康度 |

> 注意：LT 增长并不总是好事——如果 LT 增长但 DAU 下降（如屏蔽低活用户），则是假性增长。

#### 涨指标的五大方向

1. 改进召回模型，添加新的召回通道
2. 改进粗排和精排模型
3. 提升召回、粗排、精排中的多样性
4. 特殊对待新用户、低活用户等特殊人群
5. 利用关注、转发、评论这三种交互行为

---

### 8.2 改进召回模型

> 对应文件：`Slides/08_Improvement_02.pdf`

推荐系统有**几十条**召回通道，召回总量固定。双塔（Two-Tower）和 I2I（Item-to-Item）是占据大部分配额的核心模型。总量越大，指标越好，粗排计算量越大。

有很多内容池，比如30天物品、1天物品、6小时物品、新用户优质内容池、分人群内容池等。同一模型可以用于多个内容池，得到多条召回通道，不会增加训练计算量。每个内容池都需要一个内容索引，还需要线上ANN检索，增加少量计算量。

#### 改进双塔模型

**方向 1：优化正/负样本**
- 简单正样本：有点击的 (用户, 物品) 对
- 简单负样本：随机组合的 (用户, 物品) 对
- 困难负样本：排序靠后的 (用户, 物品) 对

**方向 2：改进网络结构**
- 用 DCN 替换全连接网络（用户塔/物品塔）
- 在用户塔中加入用户行为序列（Last-N）
- 使用**多向量模型**代替单向量模型（捕捉用户多兴趣）
  - 标准的双塔模型也叫单向量模型
  - 多向量模型：用户塔输出多个向量，物品塔输出单个向量，计算多对单的相似度，每个相似度对应一个指标（比如对点击率的预估、对点赞率的预估等）。
  - 区别：单向量模型是做简单的二分类；多向量模型类似排序中的多目标问题。
  - 为什么物品塔只输出一个向量？如果输出10个向量，要建立10个向量数据库，计算量太大了。

**方向 3：改进训练方法**
- 结合二分类与 batch 内负采样（热门物品出现的概率更高，需对热门物品做纠偏）
- 使用自监督学习，让冷门物品 Embedding 学得更好

#### 改进 I2I 模型

I2I 最常见用法是 **U2I2I**（用户 → 喜欢的物品 → 相似物品 → 推荐）：
- **方法 1**：ItemCF 及其变体（ItemCF、Online ItemCF、Swing、Online Swing），线上并行使用
- **方法 2**：基于物品向量（双塔/图神经网络）计算向量相似度

#### 小众召回模型

- **U2U2I**：u₁ 与 u₂ 相似，u₂ 喜欢物品 i，推荐 i 给 u₁
- **U2A2I**：用户喜欢某作者 → 该作者的物品
- **U2A2A2I**：用户 → 相似作者 → 该作者的物品
- **PDN、Deep Retrieval、SINE、M2GRL** 等更复杂的模型

> 在召回总量不变的前提下，**调整各通道配额**（甚至对不同用户群体使用不同配额），可以提升核心指标。

---

### 8.3 改进排序模型

> 对应文件：`Slides/08_Improvement_03.pdf`

#### 精排模型改进

**基座改进：**
1. **加宽加深**：增加网络宽度和深度，预测更准确（计算量增大）
2. **自动特征交叉**：引入 Bilinear / LHUC 等特征交叉
3. **特征工程**：添加统计特征、多模态内容特征

**多目标预估改进：**
1. **增加预估目标**：挖掘更多细粒度目标（进评论区、给评论点赞……）加入融分公式
2. **MMoE / PLE 结构**：可能有效，但实践中往往无效，需实验验证
3. **纠正 position bias**：曝光位置靠前天然点击率偏高，纠偏后预估更准

#### 粗排模型改进

- **模型升级**：三塔模型（COLD）替代多向量双塔，效果更好，工程实现难度较大
- **粗精排一致性建模（知识蒸馏）**：
  - **Pointwise 蒸馏**：用精排预估值与用户真实行为的融合值作为粗排训练目标
    - y是用户真实行为，p是精排的预估
    - 用(y + p) / 2 作为粗排训练目标，兼顾用户行为和精排预估
    - 实际效果比只用y效果更好
  - **Pairwise/Listwise 蒸馏**：做 Learning-to-Rank，让粗排拟合精排的物品**排名**（而非预估值）
    - 给定k个候选物品，按照精排预估做排序。做learning to rank (LTR)，让粗排拟合物品的序（而非值）。LTR鼓励粗排预估点击率跟精排预估一致，否则有惩罚。LTR通常使用pairwise logistic loss。
  - **缺点**：精排出 bug 时会污染粗排训练数据

#### 用户行为序列建模
最简单的方法是对物品向量做平均。DIN 引入注意力机制，给不同历史物品不同权重。工业界目前沿着SIM的方向发展。先用类目等属性筛选物品，然后用DIN对物品向量做加权平均。

工业界沿 SIM 方向迭代：
1. **增加序列长度**：提升准确性，增加计算量
   1. 做长非常难，快手能做到一百万多万个历史物品，几乎能覆盖用户历史所有的物品
2. **改进筛选方法**：离线用多模态神经网络提取内容特征，聚为 1000 类；线上用候选物品的聚类序号筛选历史序列，从百万级降到数千级。可能还需要做进一步筛选，继续降低数量级
3. **补充 ID 以外的特征**：序列物品表示中加入更多特征

#### 在线学习

- 全量更新 + 增量更新结合，增量更新实时跟踪兴趣变化
- **资源代价大**：线上多模型 AB 测试，每个模型需一套在线学习机器，限制模型迭代效率
  - 线上有m个模型，其中一个是holdout，一个是推全的模型，m-2个测试的新模型
  - 每套在线学习的机器成本都很大（比如10000个CPU），因此m数量很小，制约模型开发迭代效率
  - 在线学习对指标的提升巨大，但是会制约模型开发迭代效率

#### 老汤模型

每天训练 1 epoch 的老模型极难被追上：

| 问题 | 解法 |
|------|------|
| 如何快速判断新结构是否更好 | 新老结构随机初始化全连接层，用相同 n 天数据训练后比较 AUC |
| 如何快速追平老模型 | 复用老模型的 Embedding 层（包含"记忆"，学习慢）；用老模型做 teacher 蒸馏新模型 |

---

### 8.4 提升多样性

> 对应文件：`Slides/08_Improvement_04.pdf`

#### 精排多样性

- 融合兴趣分数 s 与多样性分数 d：`score = s + d`，用 MMR/DPP 计算 d
- 精排用**滑动窗口**（只考虑相邻物品）
- 打散规则：
  - **类目打散**：选中物品后，之后 5 个位置不允许同二级类目
  - **多模态打散**：全库物品预先聚为 1000 类，之后 10 个位置不允许同聚类

#### 粗排多样性

粗排关注整体多样性，不用滑动窗口：
1. 先用兴趣分数选出最高的 200 个物品直接进精排
2. 剩余 4800 个物品结合 s + d，再选 300 个进精排

#### 召回多样性

**双塔模型：往用户向量添加噪声**
- 计算用户向量后、ANN 检索前，添加随机噪声
- 用户兴趣越窄（历史类目越集中），噪声越强

**抽样用户行为序列（双塔和 U2I2I 均适用）**
1. 双塔
- 保留最近 r 个 + 从剩余中非均匀抽样 t 个，用 r+t 代替全量 n 个
- 效果：注入随机性增加多样性 + 利用更久远的历史兴趣
2. U2I2I
- U2I2I中的第一个item是指用户最近交互的n个物品之一，在U2I2I中叫做种子物品
- n个物品覆盖的类目数较少，且类目不平衡
- 做非均匀随机抽样，从n个物品中选出t个，让类目平衡。抽样得到的t个物品作为U2I2I的种子物品
- 一方面，类目更平衡，多样性更好；另一方面，n可以更大，覆盖的类目更多

#### 兴趣探索

- 保留约 **2%** 曝光坑位给非个性化推荐（从精选内容池随机抽，跳过排序直接插入）
- 短期负向，长期通过发现新兴趣带来正向影响
 ### 8.5 特殊对待特殊用户人群

> 对应文件：`Slides/08_Improvement_05.pdf`

特殊用户（新用户、低活用户）行为稀少，个性化不准，容易流失，且行为模式与主流用户不同导致通用模型预估有偏。

#### 1. 构造特殊内容池

- **方法 1**：圈定目标人群，按该人群的交互次数/交互率筛选优质物品，定期更新
- **方法 2**：用因果推断评估物品对该人群留存率的贡献，据此选物品
- 用双塔模型从特殊内容池做召回（内容池比全量小 10~100 倍，额外算力开销小）
- 新用户需单独训练召回模型

#### 2. 特殊排序策略

- 对新/低活用户少投或不投广告
- 不在新用户上做新物品探索（冷启推荐不准会伤害体验）
- **差异化融分公式**：低活用户融分时提高点击率权重；保留几个坑位专给点击率最高的物品（事实证明确实能提高低活用户的留存）

#### 3. 特殊排序模型

| 方法 | 做法 |
|------|------|
| **大模型 + 小模型** | 大模型拟合全体用户行为 p；小模型只用特殊用户数据，拟合残差 y-p；特殊用户预估 = p+q |
| **多 expert 融合** | 类似 MMoE，根据用户特征（新老/活跃度）加权融合多个 expert 的输出 |
| **大模型 + 小模型校准** | 大模型预估后，用用户特征+预估值输入小模型（如 GBDT），在特殊用户数据上训练校准 |

> **错误做法**：为每个人群单独维护一个大模型。短期有效，但长期无人维护、老化后反不如通用主模型。

---

### 8.6 利用交互行为

> 对应文件：`Slides/08_Improvement_06.pdf`

#### 关注

**对用户留存的价值**：用户关注作者数越多，留存率越高。促进低关注用户多关注：
- 融分公式加入 `w(f) · p_follow`，w(f) 随已关注数 f 单调递减
- 构造**促关注内容池**（关注率高的物品），专门用于低关注用户的召回

**对发布的价值**：粉丝数少的新作者，每增加一个粉丝提升发布积极性最大：
- 融分公式加入 `w(f_a) · p_follow`，f_a 为作者粉丝数，粉丝越少权重越大

**隐式关注**：用户反复点击某作者内容但未显式关注 → 挖掘隐式关注关系，构造 U2A2I 召回通道

#### 转发（分享回流）

简单提高转发率权重会损害点击和其他交互。**KOL 建模**更精准：
- 根据历史转发带来的站外回流量，识别**站外 KOL**
- 融分公式加入 `k_u · p_share`，站外 KOL 的 k_u 大，普通用户小
- 构造**促转发内容池**和召回通道，只对站外 KOL 生效

#### 评论

**促发布**：新物品缺少评论时提权（`w_i · p_comment`，w_i 与已有评论数负相关），让新物品尽快获得评论，提升作者发布积极性。

**促留存**：
- 喜欢互动的用户 → 构造**促评论内容池** → 提升留存
- 常留高质量评论（点赞量高）的用户 → 其评论对作者和其他用户留存有贡献 → 用排序/召回策略鼓励多留评论

---

> 更新记录：2026-03-27 完成第八章（指标体系、改进召回、改进排序、提升多样性、特殊用户策略、利用交互行为）。至此全部八章笔记完成。
